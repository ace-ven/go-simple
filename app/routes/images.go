package routes

func (a *App) SetRoutes() {
	// a.Get("/api/books", a.handleRequest(services.GetBooks))
	// a.Get("/api/books/{id}", a.handleRequest(services.GetBook))
	// a.Post("/api/books", a.handleRequest(services.CreateBook))
	// a.Put("api/books/{id}", a.handleRequest(services.UpdateBook))
	// a.Delete("api/books/{id}", a.handleRequest(services.DeleteBook))
}
